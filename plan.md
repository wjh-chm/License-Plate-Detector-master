# 基于事件数据的车牌检测改进需求书

## 1. 项目背景

当前项目基于 YOLOv5 进行车牌检测，已有检测脚本 `detect_plate.py` 和权重 `weights/best.pt`。现有模型主要面向普通图像输入，在低照度、曝光不足、强反光或过曝场景下，普通图像中的车牌区域容易丢失纹理，导致检测失败。

事件相机数据对亮度变化敏感，在低照或高动态光照变化场景中仍可能保留车辆轮廓、车牌边缘和字符变化信息。因此，本阶段目标不是追求高成功率，而是验证事件数据能否在困难光照场景下为车牌检测提供有效增益。

## 2. 项目目标

1. 设计适合现有 YOLOv5 迁移学习的事件三通道表示图。
2. 将 `dvSave-2026_04_26_22_12_37/processed.npy` 中的事件矩阵转换为可视化图像数据集。
3. 从生成的表示图中筛选适合微调的样本。
4. 使用少量标注样本对现有车牌检测模型进行微调。
5. 验证事件表示图在低照、过曝、强反光场景下是否优于普通灰度图或原始暗光图。

## 3. 输入数据

当前主要输入位于：

```text
dvSave-2026_04_26_22_12_37/
```

其中：

```text
dvSave-2026_04_26_22_12_37.aedat4  原始事件数据
processed.npy                       已转换后的矩阵数据
```

`processed.npy` 当前包含：

```text
blur1: shape = (260, 346), dtype = uint8
blur2: shape = (260, 346), dtype = uint8
e1:    shape = (12, 260, 346), dtype = float32
e2:    shape = (12, 260, 346), dtype = float32
```

说明：

`blur1` 和 `blur2` 可视为事件数据关联的灰度图或模糊重建图。

`e1` 和 `e2` 是 12 个时间片的事件体素数据，像素值包含正事件、负事件和无事件。

## 4. 推荐事件三通道表示方案

本阶段优先采用“灰度 + 事件边缘融合”的三通道表示。该方案更适合从现有普通图像 YOLOv5 权重迁移，因为它保留了普通图像外观，同时引入事件边缘信息。

### 4.1 主方案：灰度事件融合图

对 `e1` 或 `e2` 做事件累计：

```python
pos = sum(max(event, 0))      # 正事件累计
neg = sum(max(-event, 0))     # 负事件累计
event_abs = pos + neg         # 总事件强度
```

生成三通道图：

```text
R = blur2
G = event_abs
B = enhanced_blur_event
```

其中：

```text
enhanced_blur_event = max(blur2, event_abs)
```

该表示的意义：

```text
R 通道保留普通图像结构，方便 YOLO 迁移。
G 通道突出事件边缘，增强低照和强反光下的变化信息。
B 通道融合灰度和事件，强化车牌边框、车身轮廓和局部反光变化。
```

### 4.2 备选方案一：极性事件图

生成方式：

```text
R = pos
G = event_abs
B = neg
```

该方案能明确区分亮度上升和亮度下降，适合分析事件数据本身的表达能力。但它与普通图像分布差异较大，直接使用现有 YOLO 权重时迁移难度更高。

### 4.3 备选方案二：时间分段事件图

将 12 个时间片分为早、中、晚三段：

```text
R = 第 0-3 个时间片事件累计
G = 第 4-7 个时间片事件累计
B = 第 8-11 个时间片事件累计
```

该方案保留运动和时间变化信息，适合车辆运动明显或光照变化明显的场景。但作为第一阶段微调输入，不如主方案稳定。

## 5. 归一化要求

事件累计图不能直接使用最大值归一化，否则强光、反光或噪声点会压低整体对比度。

统一采用百分位归一化：

```python
upper = np.percentile(x, 99)
x = np.clip(x, 0, upper)
x = x / max(x.max(), 1e-6) * 255
x = x.astype(np.uint8)
```

对于事件强度分布特别极端的情况，可使用对数增强：

```python
x = np.log1p(x)
```

推荐优先使用：

```text
log1p + 99 分位数归一化
```

## 6. 表示图生成要求

需要新增或恢复一个事件表示图生成脚本，建议命名为：

```text
event_representation_export.py
```

脚本输入：

```text
--input dvSave-2026_04_26_22_12_37/processed.npy
--output-dir event_representations
--event-key e2
--mode fusion
```

脚本输出：

```text
event_representations/
  fusion/
    fusion_e2.png
  polarity/
    polarity_e2.png
  temporal/
    temporal_e2.png
  preview/
    comparison_sheet.png
```

最低要求：

1. 支持 `fusion`、`polarity`、`temporal` 三种模式。
2. 支持选择 `e1` 或 `e2`。
3. 输出 3 通道 `png` 图像。
4. 同时输出一张对比预览图，包含 `blur1`、`blur2`、主方案、备选方案。
5. 保存生成参数到 JSON，便于复现实验。

## 7. 微调样本选择标准

不建议随机选择所有表示图。第一批微调样本应优先选择“人眼能大致框出车牌”的样本。

### 7.1 正样本选择标准

优先选择：

```text
1. 普通暗光图中车牌难以看清，但事件融合图中车牌边缘更明显的图。
2. 车辆轮廓完整，车牌大致位置可判断的图。
3. 车牌区域存在边框、字符边缘、反光变化或局部亮度变化的图。
4. 低照、过曝、强反光等困难场景。
```

暂不选择：

```text
1. 人眼完全无法判断车牌位置的图。
2. 背景噪声明显强于车辆和车牌的图。
3. 车辆或车牌只出现极小一部分的图。
4. 事件图中只剩大灯或强噪声，没有稳定车牌区域的图。
```

### 7.2 负样本选择标准

需要保留少量负样本，避免模型把强光、边缘噪声、车灯误检为车牌。

负样本包括：

```text
1. 有车辆但无可见车牌。
2. 有强光或反光但没有车牌。
3. 只有背景道路、树木、灯光变化的图。
```

### 7.3 第一批样本比例

建议第一批标注 80 到 200 张图。

推荐比例：

```text
60% 车牌相对清楚的事件融合图
25% 低照、过曝、强反光困难图
15% 无车牌或极难负样本
```

如果样本数量不足，可以先标注 30 到 50 张，验证微调是否带来明显改善。

## 8. 标注要求

标注格式采用 YOLO 格式：

```text
class_id center_x center_y width height
```

坐标均归一化到 0 到 1。

类别暂定：

```text
0 plate
```

标注原则：

1. 只标注人眼可以合理判断的车牌区域。
2. 如果车牌边缘不完整，但位置明确，可以按可见区域框出。
3. 如果车牌被强光完全淹没，无法判断边界，不作为正样本。
4. 同一场景不同表示图不要大量重复加入训练集，避免模型过拟合单一场景。

## 9. 微调策略

基于现有权重：

```text
weights/best.pt
```

进行小学习率微调。

建议训练配置：

```text
img-size: 640 或 800
batch-size: 根据显存设置
epochs: 30 到 80
learning-rate: 使用较小学习率
pretrained: weights/best.pt
classes: 1
```

第一轮微调只使用主方案 `fusion` 图，不混合多种表示。等主方案有效后，再比较 `polarity` 和 `temporal`。

## 10. 验证方案

需要划分训练集和验证集。验证集必须包含没有参与训练的低照、过曝、强反光样本。

重点比较：

```text
1. 原始暗光图检测结果
2. blur1/blur2 检测结果
3. fusion 事件融合图检测结果
4. polarity 极性事件图检测结果
5. temporal 时间事件图检测结果
```

评价标准：

```text
1. 是否能检测出车牌框。
2. 检测框是否覆盖真实车牌区域。
3. 强光、车灯、背景边缘是否被误检为车牌。
4. 在普通图失败的场景中，事件表示是否产生有效检测。
```

第一阶段不强制追求高 mAP，以“困难场景下出现有效检测”为主要验收目标。

## 11. 验收标准

第一阶段验收：

```text
1. 可以从 processed.npy 稳定生成 fusion、polarity、temporal 三种表示图。
2. 生成图中车辆轮廓和车牌区域可人工检查。
3. 完成至少 30 到 50 张事件融合图标注。
4. 完成一次基于 weights/best.pt 的微调。
5. 在至少若干张普通暗光图失败的样本上，事件融合图能产生车牌候选框。
```

第二阶段验收：

```text
1. 标注样本扩展到 80 到 200 张。
2. 建立固定验证集。
3. 对 fusion、polarity、temporal 三种表示进行对比。
4. 明确选出最适合本项目的事件表示方式。
5. 输出检测结果 JSON 和可视化图，方便后续 OCR 或人工校正。
```

## 12. 风险与注意事项

1. 事件图不一定适合直接 OCR。事件数据更适合增强检测定位，OCR 仍建议结合灰度图或增强后的车牌裁剪图。
2. 如果车牌和相机都静止，且没有局部亮度变化，事件数据可能无法提供足够字符信息。
3. 强光和反光会产生大量事件，需要通过百分位归一化、对数增强和负样本训练抑制误检。
4. 不能只挑最好看的图训练，否则模型会在真实困难场景中泛化差。
5. 不建议第一阶段同时混合多种事件表示训练，应先固定一种主表示，验证有效后再扩展。

## 13. 推荐实施顺序

1. 编写事件表示图生成脚本。
2. 从 `processed.npy` 生成三种表示图和对比预览图。
3. 人工检查主方案 `fusion` 是否稳定显示车辆和车牌区域。
4. 扩展生成更多时间窗口或更多样本。
5. 选取 30 到 50 张最有代表性的 `fusion` 图进行标注。
6. 使用 `weights/best.pt` 小学习率微调。
7. 在未参与训练的困难样本上测试。
8. 若有效，再扩展到 80 到 200 张样本，并对比三种表示方案。

## 14. 当前推荐结论

本项目当前最推荐的第一版事件表示为：

```text
R = blur2
G = log1p(event_abs)
B = max(blur2, log1p(event_abs))
```

其中 `event_abs` 来自 `e2` 的 12 个时间片累计。若 `e2` 的噪声过强，再切换到 `e1` 对比。

该方案的优势是保留普通图像结构，同时利用事件边缘增强低照、过曝和强反光条件下的车牌区域，是最适合从现有 YOLOv5 权重开始微调的路线。
